project(${PROJECT_NAME_GLOBAL}ADSBReceiver)

install(DIRECTORY ${PROJECT_SOURCE_DIR}/dump1090 DESTINATION ${BINDIR})

if(USE_SHARED_OUTPUT)
	file(COPY ${PROJECT_SOURCE_DIR}/dump1090 DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
endif()

add_lib_static(${PROJECT_NAME} ${deps})
